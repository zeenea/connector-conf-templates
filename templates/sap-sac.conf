name =
code =
connector_id = "sap-sac"
connection {
    # SAP SAC api url (example : "https://example.eu10.hana.ondemand.com")
    url =

    oauth {
        # SAP SAC OAuth endpoint (example : "https://example.authentication.eu10.hana.ondemand.com/oauth/token")
        endpoint = 
        client_id =
        client_secret =
    }
}
