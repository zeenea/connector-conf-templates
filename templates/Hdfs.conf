name = 
code = 
connector_id = "Hdfs"

connection {
    hadoop_conf_dir = "/etc/hadoop/conf"
    # Connection principal
    principal = null
    # Keytab file path
    keytab = null
}

# Inventory Root
# Path from where dataset inventory starts. 
# Only subfolders of root will be parsed.
inventory.root = "/"

# Use new XML namespace identification, replace with value "legacy" if XML were imported from the connection before.
xml.namespace_identification = "uri"
